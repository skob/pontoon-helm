replicaCount: 1

imagePullSecrets:
  - name: regcred

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  name: ""

podSecurityContext:
  fsGroup: 1000

securityContext:
  # capabilities:
  #   drop:
  #   - ALL
  readOnlyRootFilesystem: false
  runAsNonRoot: true
  runAsUser: 1000

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: true
  # className: nginx
  annotations:
    kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: pontoon.zt-sel.k8s.jwi
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

postgresql-ha:
  nameOverride: postgresql
  fullnameOverride: postgresql
  postgresql:
    database: pontoon
    password: 6lBz7pfzUR
    repmgrPassword: W34h5dXxaT
  pgpool:
    adminPassword: a3UfXbtMAK

rabbitmq:
  nameOverride: rabbit
  fullnameOverride: rabbit
  auth:
    username: admin
    password: secret

env:
  open:
    SITE_URL: pontoon.zt-sel.k8s.jwi
    ALLOWED_HOSTS: pontoon.zt-sel.k8s.jwi
  secrets:
    DATABASE_URL: postgres://postgres:6lBz7pfzUR@postgresql/pontoon
    BROKER_URL: amqp://admin:secret@rabbit:5672//
    SECRET_KEY: T7IFqwm32CoFiNjTvVHx0EEjZaIgL
